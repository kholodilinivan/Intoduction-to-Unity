using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ScriptsControl : MonoBehaviour
{
    public int score = 5;
    // Start is called before the first frame update
    void Start()
    {
        // Create public int variable “score” in one script and change it from another script “ColorControl” (both scripts
        // are attached to a similar game object)
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
